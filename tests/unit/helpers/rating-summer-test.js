import {
  ratingSummer
} from '../../../helpers/rating-summer';
import { module, test } from 'qunit';

module('RatingSummerHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = ratingSummer(42);
  assert.ok(result);
});
