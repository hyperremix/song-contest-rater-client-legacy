import {
  incrementIndex
} from '../../../helpers/increment-index';
import { module, test } from 'qunit';

module('IncrementIndexHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = incrementIndex(42);
  assert.ok(result);
});
